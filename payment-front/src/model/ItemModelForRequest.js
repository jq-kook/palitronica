export default class ItemModelForRequest {
  constructor (itemId, name, price, quantity) {
    this.itemId = itemId
    this.name = name
    this.price = price
    this.quantity = quantity
  }
}
